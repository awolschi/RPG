Make it possible to set a skill to the standard attack button.

The first skill that a class gets at level 1, does not show up after loading a savefile. That should not happen.

Control buttons in the combat screen overlap with the reputation exp bar. 
Also the text of goblin familiar colides with the dungeon room number. Can you make that better? Also the exp bars in the combat screen should be a bit taller. For now the white numbers are bigger than the bar, so they are hard to read.

